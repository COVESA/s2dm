{
    "name": "s2dm",
    "version": "0.1.0",
    "description": "A friendly approach for modeling semantic data.",
    "scripts": {
        "install-deps": "npm install",
        "graphql-inspect": "graphql-inspector"
    },
    "dependencies": {
        "graphql": "^16.8.1",
        "@graphql-inspector/cli": "^5.0.8"
    },
    "engines": {
        "node": ">=16.0.0",
        "npm": ">=7.0.0"
    }
}
